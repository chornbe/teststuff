name: GitHub Actions Demo
run-name: ${{ github.actor }} -> merging ${{ github.ref }}
on: [merge]
jobs:
 Explore-GitHub-Actions: 
    runs-on: ubuntu-latest
    steps:
        uses: actions/checkout@v3
          ls ${{ github.workspace }}
      - run: echo "🍏 This job's status is ${{ job.status }}."
